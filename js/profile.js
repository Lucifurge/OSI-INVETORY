import {supabase,shell,setApp,esc,toast,qs,roleName,logActivity} from "./app.js";
const p=await shell("My Profile");if(!p)throw 0;
setApp(`<div class="max-w-3xl"><h1 class="text-3xl font-black">My Profile</h1><p class="text-slate-500 mb-6">Update your account details and password.</p>
<div class="card p-6 mb-5"><h2 class="text-xl font-black mb-4">Account</h2><form id="profile" class="grid sm:grid-cols-2 gap-4"><div><label class="label">Full name</label><input class="input" name="full_name" required value="${esc(p.full_name)}"></div><div><label class="label">Email</label><input class="input bg-slate-50" disabled value="${esc(p.email||"")}"></div><div><label class="label">Role</label><input class="input bg-slate-50" disabled value="${esc(roleName(p))}"></div><div><label class="label">Status</label><input class="input bg-slate-50" disabled value="${esc(p.status)}"></div><button class="btn btn-primary sm:col-span-2">Save profile</button></form></div>
<div class="card p-6"><h2 class="text-xl font-black mb-4">Change password</h2><form id="password" class="space-y-4"><div><label class="label">New password</label><input class="input" type="password" name="password" minlength="8" required autocomplete="new-password"></div><div><label class="label">Confirm new password</label><input class="input" type="password" name="confirm" minlength="8" required autocomplete="new-password"></div><button class="btn btn-primary">Change password</button></form></div></div>`);
qs("#profile").onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));const r=await supabase.from("profiles").update({full_name:d.full_name.trim(),updated_at:new Date().toISOString()}).eq("id",p.id);if(r.error){toast(r.error.message,false);return}const {data:{user}}=await supabase.auth.getUser();if(user) await supabase.auth.updateUser({data:{full_name:d.full_name.trim()}});toast("Profile updated")};
qs("#password").onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));if(d.password!==d.confirm){toast("Passwords do not match.",false);return}const {error}=await supabase.auth.updateUser({password:d.password});if(error){toast(error.message,false);return}await logActivity("Changed password","profiles",p.id,{});e.target.reset();toast("Password changed successfully")};
