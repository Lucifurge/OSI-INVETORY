import {supabase,shell,setApp,esc,fmtDate,toast} from "./app.js";
const p=await shell("Activity Log");if(!p)throw 0;
if(!["admin","super_admin"].includes(p.roles?.name)){setApp(`<div class="card p-8"><h1 class="text-2xl font-black">Admin access required</h1></div>`);throw 0}
async function load(){const {data,error}=await supabase.from("activity_log").select("*,profiles:user_id(full_name,email)").order("created_at",{ascending:false}).limit(300);if(error)throw error;setApp(`<div class="mb-5"><h1 class="text-3xl font-black">Activity Log</h1><p class="text-slate-500">Recent application actions.</p></div><div class="card table-wrap"><table class="data-table"><thead><tr><th>Date</th><th>User</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>${(data||[]).map(x=>`<tr><td>${fmtDate(x.created_at)}</td><td>${esc(x.profiles?.full_name||"System")}</td><td class="font-bold">${esc(x.action)}</td><td>${esc(x.entity_type||"—")}</td><td><code class="text-xs">${esc(JSON.stringify(x.details||{}))}</code></td></tr>`).join("")||`<tr><td colspan="5" class="text-center py-8 text-slate-500">No activity.</td></tr>`}</tbody></table></div>`)}
load().catch(e=>toast(e.message,false));
